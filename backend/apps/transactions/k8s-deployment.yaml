apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-1-transactions
  labels:
    app: ms-1-transactions
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ms-1-transactions
  template:
    metadata:
      labels:
        app: ms-1-transactions
    spec:
      containers:
      - name: ms-1-transactions
        image: cardinal/ms-1-transactions:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: host
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: password
        - name: DB_DATABASE
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: database
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ms-1-transactions-service
spec:
  selector:
    app: ms-1-transactions
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
