'use strict';

var _express = require('express');

var _express2 = _interopRequireDefault(_express);

var _ = require('./');

var _bodyParser = require('body-parser');

var _bodyParser2 = _interopRequireDefault(_bodyParser);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var app = (0, _express2.default)();
var serviceName = 'service-name';
var Logger = (0, _.initLogger)(serviceName);

app.use(_bodyParser2.default.json());
app.use((0, _.loggerMiddleware)(serviceName));

Logger.log('info', 'Some message here...');

app.get('/', function (req, res, next) {
  res.write('This is a normal request, it should be logged to the console too');
  res.end();
});

app.listen(3000, function () {
  console.log("aws-cloudwatch-logger demo listening on port %d in %s mode", this.address().port, app.settings.env);
});